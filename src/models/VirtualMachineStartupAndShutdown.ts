
export interface VirtualMachineStartupAndShutdown {
  Autostart: string;
  "Autostart delay": number;
  Autostop: string;
  "Startup view": string;
  "On shutdown": string;
  "On window close": string;
  "Pause idle": string;
  "Undo disks": string;
}
